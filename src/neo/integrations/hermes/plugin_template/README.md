# Neo Hermes Memory Provider

This is a thin Hermes plugin shim. The real provider implementation lives in the installed `neo-agent-knowledge` Python package at `neo.integrations.hermes.provider`.

Configure provider behavior in `$HERMES_HOME/neo.json`.
